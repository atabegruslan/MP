<!--index.wxml-->
<view class="container">
  <scroll-view wx:for="{{posts}}">
    <text>{{item.title}}</text>
    <text> - </text>
    <text>{{item.body}}</text>
  </scroll-view>
  <button bindtap="createPost">Create Post</button>
</view>
